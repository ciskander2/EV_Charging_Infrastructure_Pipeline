# Core ETL dependencies
boto3>=1.34,<2
pandas>=2.2,<3
pyarrow>=15,<18
requests>=2.31,<3
s3fs>=2024.3.0
# Database load dependencies
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9,<3
# Local development convenience
python-dotenv>=1.0,<2
# Airflow is supplied by the apache/airflow Docker image in docker-compose.yaml.
# Install this locally only if you want to import/test DAGs outside Docker:
# apache-airflow==2.9.3
